import { Delete{{pascalCase name}}FromDatabaseRepository } from '@/domain/usecases/{{dashCase name}}/delete-{{dashCase name}}-from-database/protocols';
import { prismaConnector } from '@/infra/databases/postgres/prisma';
import { PrismaClient } from '@prisma/client';

export class PrismaDelete{{pascalCase name}}FromDatabaseRepository
  implements Delete{{pascalCase name}}FromDatabaseRepository
{
  private prismaConnection: PrismaClient;

  constructor() {
    this.prismaConnection = prismaConnector.connect();
  }

  async delete(
    {{camelCase name}}Params: Delete{{pascalCase name}}FromDatabaseRepository.Params
  ): Promise<Delete{{pascalCase name}}FromDatabaseRepository.Result> {
    const { id } = {{camelCase name}}Params;

    await this.prismaConnection.{{camelCase name}}.delete({
      where: { id },
    });
  }
}
